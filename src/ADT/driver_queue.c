#include "queue.h"

/*
int main(){
    Queue game;
    CreateQueue(&game);
    enqueue(&game , "MAR");
    enqueue(&game , "VEL");
    enqueue(&game , "MARVEL");
    enqueue(&game , "TIM");
    displayQueue(game);
    if(isMember(game , "MARVEL")){
        printf("Berhasil\n");
    }
    else{
        printf("Gagal\n");
    }
   return 0;
}
*/